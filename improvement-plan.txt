AFMS Improvement Plan
Date: 2026-03-18

Priority 6 — Clean up small UI data issues
- Remove duplicate airport datalist options and normalize naming in Views/Home/AdvancedSearch.cshtml.
- Standardize status labels and button text across the add/edit forms and tables.
- Keep the UI tweaks small and consistent.

Priority 7 — Trim repeated pagination/render logic
- Materialize pagination pages once per render where the same sequence is reused.
- Consolidate URL building for advanced search results if possible.
- This is a small cleanup, not a functional change.

Priority 8 — Revisit startup schema patch
- Replace the raw ALTER TABLE startup patch in Program.cs with a migration-based approach or a clearer startup migration step.
- Keep this lower priority because the app already builds and runs.

